---
date: 2026-04-24
generated_by: "weekly-content-gap-audit"
---

# PickupRoster — Content Gap Audit (2026-04-24)

Third pass this week. Since the 2026-04-23 audit (`content/_queue/gap-audit-2026-04-23.md`) the team shipped a rain-day blog post (`4ff6c6f`), the SIS roster-import guide (`0facc00`), workstream-4 ops-runbook + dismissal-day checklist (`39d54e9`), and a string of security/a11y hardenings (P0-1 headers `8c7dd44`, P0-2 CSV importer `2b14085`, P0-3 PBKDF2 `1cb9cfa`, P0-5 audit overrides `2d272d9`). Drill content is still the dominant hole; two new opportunities surfaced this run — a trust/security explainer to surface the P0 work, and a viewer-access guide that's been ignored across all three audits.

## Features with no documentation

Surfaces with a live route and zero blog or guide:

- **Drill templates end-to-end** — `app/routes/admin/drills.library.tsx`, `admin/drills.$templateId.tsx`, `admin/drills.$templateId.run.tsx`, `drills.live.tsx`. Shipped `acfcedb`, mentioned in the April showcase, third audit running with no guide.
- **Admin history & reports** — `app/routes/admin/history.tsx`. No "reading your dismissal report" walkthrough.
- **Homeroom & student management** — `homerooms.tsx`, `homerooms.$id.tsx`, `create/create.*.tsx`, `edit/edit.*.tsx`. The CSV import guide (`2026-04-24-importing-student-rosters-from-sis.md`) covers bulk-load, but the day-to-day add/edit/move flow is still undocumented.
- **Viewer access** — `app/routes/viewer-access.tsx`. The read-only carline display is a pilot buy-trigger; never explained externally.
- **Print surfaces** — `admin/print.board.tsx`, `print.master.tsx`, `print.homeroom.$teacherId.tsx`, `print.drills.$templateId.tsx`. The paper-backup story.
- **Public status page** — `app/routes/status.tsx`. Showcased, no standalone post.
- **Self-serve password reset** — `app/routes/auth/forgot-password.tsx` + `auth/reset-password.tsx`. Mentioned in showcase, no guide for end users.
- **Billing + plan usage** — `pricing.tsx`, `admin/billing.tsx`, `billing-required.tsx`. No "picking the right plan" explainer.
- **FAQs page** — `app/routes/faqs.tsx`. Route exists; body content not in repo.

## Blog topic gaps

Still-uncovered topics, ranked by how well they pair with shipped or near-shipped features:

- Running lockdown drills — directly tied to drill templates + R2 silent-lockdown-mode (`docs/nightly-queue.md`).
- Fire-drill and reunification playbooks — pairs with R1 drill-accountability-roster + R3 compliant-drill-log-export.
- Morning arrival / drop-off (mirror of the car-line post).
- Walker, bike, and bus-loop coordination with the car line.
- Kindergarten first-week dismissal.
- ADA and mobility-impaired pickup lanes.
- After-school-care handoff and the late-pickup policy.
- Staffing ratios for dismissal.
- Trust/security explainer — newly relevant given the four P0 hardenings landed this week (`docs/security-review-2026-04-24.md`).

## Recent shippings needing showcase

Last-30-day commits without a content footprint beyond the existing April showcase:

- `acfcedb feat: drill templates` — showcased, still no standalone guide.
- `39d54e9 docs(ops): runbook + dismissal-day front-desk checklist` — workstream 4 marked `[x]` in `docs/nightly-queue.md`. The internal `docs/dismissal-day-checklist.md` is exactly the kind of thing that becomes a high-value public guide with light editing.
- `8c7dd44 P0-1 security headers` + `1cb9cfa P0-3 PBKDF2 600k` + `2b14085 P0-2 CSV importer hardening` + `2d272d9 P0-5 npm audit overrides` — four security improvements, zero external surfacing. Strong candidate for a single "how PickupRoster handles your data" trust post that doubles as sales material when paired with the still-pending legal-pages workstream (`docs/nightly-queue.md` P1.2).
- `73d04ad docs(research): support-contact spec` — pre-launch; warrants a teaser once shipped.
- `f7a1f02 fix(signup): step-1 bounce-back` + `255f304 fix(signup,a11y)` + `68ef5c9 fix(marketing,a11y)` — polish; fold into next changelog, not standalone.

## Priority ranking — top 5 next pieces

1. **Guide — "Setting up your first drill template"** (guide). Carried for the third audit in a row. Biggest shipped-but-undocumented surface; blocks adoption of `acfcedb` and prepares the ground for R1/R2/R3 launch posts.
2. **Blog — "Running a lockdown drill without scaring the kids: a principal's checklist"** (blog). High-intent SEO, maps 1:1 to drill templates, pre-announces R2 silent-lockdown-mode.
3. **Guide — "Sharing the parent-facing carline display"** (guide). Covers `app/routes/viewer-access.tsx` — a pilot buy-trigger that has been ignored across all three audits this week.
4. **Blog — "How PickupRoster handles your students' data"** (blog). Surfaces the four P0 security wins (`docs/security-review-2026-04-24.md`), pairs with the soon-to-ship legal pages, and is the easiest trust-building post for prospective districts.
5. **Guide — "Reading your daily dismissal report"** (guide). Covers `app/routes/admin/history.tsx`; second-most-likely support question after roster setup.

## Notes

The drill-templates guide has now been the #1 recommendation for three consecutive audits. Recommend assigning it to the next content run before R1 ships and forces the issue. The new entry on the list is the trust/security post — it's cheap to write (the hardening commits are well-documented in `docs/security-review-2026-04-24.md`) and provides cover for the still-blocked legal-pages workstream.
