---
date: 2026-07-24
generated_by: "weekly-content-gap-audit"
---

# Content Gap Audit — PickupRoster (2026-07-24)

Audited every route under `app/routes/`, the last 30 days of `git log`, and all
published blog posts, guides, and showcase entries. The catalog is strong on
car-line strategy and the drills feature, but several shipped features — some
core to the product — have no dedicated documentation, and the most recent 30
days of work is almost entirely undocumented.

## Features with no documentation

These routes power real workflows but have no matching guide or blog post:

- **Classrooms & teacher accounts** (`admin/classrooms.tsx`,
  `admin/classrooms.$classroomId.tsx`) — TEACHER-role users and per-classroom
  teacher assignment shipped in June. No guide explains creating classrooms or
  what a teacher account can and can't do.
- **Households & duplicate merge** (`admin/households.tsx`,
  `admin/households.duplicates.tsx`, `admin/students.duplicates.tsx`) — the
  merge screen is referenced in a showcase but never documented as a workflow.
- **Viewer access / live viewer roster** (`viewer-access.tsx`,
  `_components/LiveViewerRoster.tsx`) — reusable magic links with 300-day
  school-year sessions. No guide or blog, despite being a headline July ship.
- **Print center** (`admin/print.*` — board, master, homeroom, drills) — five
  print routes, zero documentation on producing paper fallbacks.
- **District setup** (`district/*`) — a "district rollouts" blog post exists,
  but no how-to guide for a district admin setting up schools, admins, or
  billing across campuses.

## Blog topic gaps

Adjacent topics competitors cover that the blog does not: bus/dismissal-mode
coordination (walkers vs. riders vs. car line), accessibility and special-needs
dismissal accommodations, end-of-year/roster-rollover playbooks, and a
security/data-handling explainer for IT buyers (the June CSV-formula-injection
and Sentry-scrubbing hardening is a natural anchor for a trust-focused post).

## Recent shippings needing showcase

The July showcase (dated 07-02) predates the biggest July ships. Missing from
any showcase/blog/guide:

- **Viewer-access magic link + 300-day sessions** (07-03) — user-facing, undocumented.
- **Classroom detail + TEACHER accounts** (06-25) and **assign any org user as
  teacher** (06-27) — mentioned only in passing.
- **Security hardening** (06-25) — Sentry event scrubbing + CSV formula-injection
  neutralization. Good trust-signal material, unmentioned.
- **Performance pass** (06-30) — homeroom N+1 fix, batched imports, session
  round-trip cuts. Worth a "why the board feels faster" note.

## Priority ranking — next 5 pieces to write

1. **Guide — "Setting up classrooms and teacher accounts"**
   Core June feature with zero docs; teachers are a new user type admins need to
   understand. *(guide)*
2. **Showcase — August catch-up: viewer access, teacher accounts, faster board**
   Three user-facing ships have escaped every showcase; close the gap. *(showcase)*
3. **Guide — "Give staff a live roster with viewer magic links"**
   Headline July feature (300-day sessions) with no how-to. *(guide)*
4. **Blog — "How PickupRoster handles your student data"**
   Turn June security hardening into an IT/buyer trust post; pairs with existing
   FERPA content. *(blog)*
5. **Guide — "Merging duplicate students and households after import"**
   Natural follow-on to the rebuilt roster importer; the merge flow is currently
   undocumented. *(guide)*

---

**File:** `content/_queue/gap-audit-2026-07-24.md`
**#1 recommendation:** Write a guide, *"Setting up classrooms and teacher
accounts"* — the June classrooms/TEACHER-account feature is core, user-facing,
and completely undocumented.
