---
date: 2026-07-14
generated_by: "weekly-content-gap-audit"
---

# PickupRoster — Content Gap Audit (2026-07-14)

**This audit ran one day after the last one** (`gap-audit-2026-07-13.md`). A weekly job firing on consecutive days is a scheduling problem, not a content one — worth checking the cron before the next cycle. Consequently there is almost nothing new to report, and this document says so rather than inventing movement. The substantive analysis in yesterday's audit stands unchanged; treat this as a delta.

## What actually changed since 2026-07-13

- **Commits:** none. `git log --since="2026-07-12"` is empty. The last shipped commit remains `9f04520b` (2026-07-03, reusable viewer magic links + 300-day sessions).
- **Content:** none added. `content/blog/2026-07-13-standardizing-dismissal-across-campuses.md` was already on disk during yesterday's run.
- **Working tree:** uncommitted changes are present in `app/components/Page.tsx`, `app/root.tsx`, `app/routes/_index.tsx`, `app/routes/admin/households.$householdId.tsx`, and — notably — `public/locales/es/admin.json` and `public/locales/es/roster.json`. Spanish locale work is in progress but unshipped. Nothing to document yet; flagging it because it lands directly on a long-carried blog gap (see below).

## Features with no documentation

Unchanged from 2026-07-13. Of 84 route modules under `app/routes/`, the customer-facing surfaces with no blog or guide:

- **Viewer access / parent-facing carline display** — `app/routes/viewer-access.tsx`, `app/routes/_components/LiveViewerRoster.tsx`. **Ninth consecutive audit.** `9f04520b` shipped reusable magic links and 300-day sessions specifically so a school can hand this out once per school year; there is still no guide telling anyone that.
- **Teachers & classrooms** — `admin/classrooms.tsx`, `admin/classrooms.$classroomId.tsx`, `admin/teachers.import.tsx`. Whole June feature area, zero docs.
- **Duplicate detection & merge** — `admin/students.duplicates.tsx`, `admin/households.duplicates.tsx`.
- **Live drill operation** — `drills.live.tsx` + instant takeover (`cd04290c`). Setup is documented; *running* a drill is not.
- **Drill history / replay** — `admin/drills.history.tsx`, `admin/drills.history.$runId.tsx`.
- **Print surfaces** — the five `admin/print.*` routes. Showcase-only.
- **District setup how-to** — `district/schools.new.tsx`, `district/admins.tsx`, `district/billing.tsx`.
- **Public status page** — `app/routes/status.tsx`.
- **Self-service profile** — `admin/profile.tsx`, `district/profile.tsx`.

**Carried accuracy risk:** `content/guides/2026-04-24-importing-student-rosters-from-sis.md` still describes the pre-June importer (old mapping flow, `Create missing homerooms` checkbox, `Undo this import`, 5 MB limit). The importer was rebuilt in June (`54e60c90`, `9328a28d`, `cee19363`). This remains the single most likely wrong page in the repo, and it is a high-traffic setup page. A day has passed and it is still wrong.

## Blog topic gaps

No topics closed. Still open: lockdown drills without scaring kids; fire-drill and reunification playbooks; **bilingual / multilingual front-office operations** (now five-plus audits, and the uncommitted `locales/es/*` work makes this the topic most likely to become shippable soonest); morning drop-off; walker/bike/bus loops; kindergarten first week; ADA-accessible lanes; after-school-care handoff; staffing ratios.

## Recent shippings needing showcase

Unchanged. Two items from the 30-day window still sit in no artifact:

- **`9f04520b` reusable viewer magic links + 300-day sessions** — landed 07-03, one day after the July showcase (`content/showcase/2026-07-showcase.md`, dated 07-02).
- **`1b639096` Sentry event scrubbing + CSV formula-injection fix** — a genuine security-posture story, absent from both the showcase and the FERPA post.

## Priority ranking — top 5 next pieces

Identical to yesterday, because nothing moved.

1. **Guide — "Sharing the carline display: viewer links for the whole school year."** (`viewer-access.tsx`; cashes in `9f04520b`.) Ninth carry. Write it or formally kill it — a backlog item that survives nine audits is not a backlog item.
2. **Guide — rewrite "Importing student rosters from your SIS."** Correctness fix, not a new piece. Fold duplicate/merge in.
3. **Guide — "Teachers and classrooms: accounts, imports, and assignments."**
4. **Guide — "Running a live drill day."** (`drills.live.tsx` + takeover.)
5. **Blog — "Running a lockdown drill without scaring the kids."** Highest-intent open SEO topic.

## Notes

Two audits in two days produced two identical priority lists. The bottleneck is not gap detection — it is that nothing on the list gets written. Recommend fixing the schedule, then picking #1 or #2 and shipping it before the next run.
