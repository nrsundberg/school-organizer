---
date: 2026-05-22
generated_by: "weekly-content-gap-audit"
---

# PickupRoster — Content Gap Audit (2026-05-22)

One week since the 2026-05-16 audit. One piece shipped: the "From clipboards to car lines" transition blog (`content/blog/2026-05-18-from-clipboards-to-carlines-digital-transition-guide.md`) — a net-new change-management topic that was not on last week's top five. All five carried recommendations remain unwritten. The codebase has now been quiet for over three weeks: the newest commit of any kind is `c46241cc` (2026-04-29), so the 30-day shipping window (2026-04-22 to 2026-05-22) contains exactly the same feature set as the last two audits. With engineering paused, this is the cleanest window content will get to close its backlog before the next batch of shippings opens new gaps.

## Features with no documentation

Of 77 route modules under `app/routes/`, the customer-facing surfaces still missing a blog or guide are:

- **Viewer access / parent-facing carline display** — `app/routes/viewer-access.tsx` (plus `_components/LiveViewerRoster.tsx`). Carried six audits; the longest-running gap in the queue.
- **Live drill operation** — `app/routes/drills.live.tsx`. The drill-template guide (2026-04-24) covers setup only; running a live drill with WS sync, presence and the activity feed has no walkthrough.
- **History & dismissal reports / replay** — `app/routes/admin/history.tsx`, `admin/drills.history.tsx`, `admin/drills.history.$runId.tsx`. Replay timeline (`ba4e7025`) is undocumented.
- **Print surfaces** — `admin/print.board.tsx`, `print.master.tsx`, `print.homeroom.$teacherId.tsx`, `print.drills.$templateId.tsx`, `print.index.tsx`. Print hub (`9d601864`) appears only in the May showcase.
- **Self-service profile** — `admin/profile.tsx`, `district/profile.tsx` (shipped `05818e81`). No guide.
- **District setup how-to** — `district/schools.new.tsx`, `district/admins.tsx`, `district/billing.tsx`. The April rollout blog sells the strategy; no step-by-step exists.
- **Audit log & impersonation visibility** — `district/audit.tsx` (`598c01d6`). Trust-relevant, undocumented.
- **Magic-link staff invites** — `accept-invite.tsx`, `set-password.tsx`. No end-user explainer.
- **Public status page** — `app/routes/status.tsx`. Carried.

## Blog topic gaps

Carried, none addressed: lockdown drills without scaring kids; fire-drill and reunification playbooks; multilingual / bilingual front-office operations (pairs with `50756ffb`, `ce60a737`, `0a3a6881`); real-time coordination across the front office; and a trust/security explainer on student-data handling (carried four audits). Seasonal evergreen topics still open: morning drop-off, walker/bike/bus loops, kindergarten first week, ADA-accessible lanes, after-school-care handoff, and dismissal staffing ratios.

## Recent shippings needing showcase

The 30-day window is unchanged from the last two audits — no code has landed since `c46241cc`. Features that reached production but never got a blog or guide: `9d601864` print hub, `05818e81` self-service profile, `598c01d6` impersonation visibility + IP/UA audit capture, `0a3a6881` public language switcher, `94ac5abb` family-owned space numbers + drill soft-delete, `f9ebae6f` real-time drill sync, `b3936202` the drill instructions/sign-off/attestation editor, and `c46241cc` the in-app upgrade flow (the May 4 billing post covers philosophy, not the new screens). All are showcase-only or research-note-only.

## Priority ranking — top 5 next pieces

1. **Sharing the parent-facing carline display** — guide. Covers `viewer-access.tsx`. Sixth audit running as #1; bounded scope, direct demo value. Ship it this week or formally drop it from the queue.
2. **How PickupRoster protects your students' data** — blog. Pulls together audit logging (`598c01d6`), Stripe Connect billing, and the status page into one trust explainer; directly useful for district sales; carried four audits.
3. **Setting up your district: schools, admins, and billing** — guide. Covers `district/schools.new.tsx`, `district/admins.tsx`, `district/billing.tsx` — the missing how-to behind the April rollouts blog.
4. **Running a live drill day** — guide. Covers `drills.live.tsx`, the core feature: setup is documented, live operation is not. New to the top five this week.
5. **Running a lockdown drill without scaring the kids** — blog. High-intent SEO; maps to drill templates plus live mode; carried.

## Notes

The viewer-access guide has now been the #1 recommendation for six consecutive audits without shipping. The reasoning still holds — it is bounded and high-value — but six weeks of carry suggests either a hidden blocker or a quiet disagreement on priority; the team should resolve that explicitly rather than carry it a seventh time. "Running a live drill day" is promoted into the top five this round because `drills.live.tsx` is the product's largest and most central route yet has no operational guide; the bilingual front-office blog sits just below the cut and should be next in line.
