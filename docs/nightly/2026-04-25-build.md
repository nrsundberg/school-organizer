# Nightly build — 2026-04-25

**Status:** PLAN (gate + summary appended at the end of the run).
**Agent:** nightly-build (scheduled).
**Workstream:** Priority 0, item **0d.2 — `interaction-tests-signup-to-paid`** in `docs/nightly-queue.md`.
**Branch:** `nightly-build/2026-04-25-0d2-signup-to-paid`.
**Base:** `origin/master` @ `9928b07d` (`fix: scope tenant uniqueness by orgId`).

## Why this workstream

Strict top-of-queue is **0d-prereq — `seeded-tenant-e2e-harness`**, but its
substance has already shipped (see `e2e/fixtures/seeded-tenant.ts` — the
admin-roster + viewer-pin specs prove it real). The polish agent flagged it
as "effectively done" in `docs/nightly/2026-04-25-polish.md`. Last night's
build (2026-04-24) shipped 0d.1 (dismissal) and is awaiting Noah's morning
gate.

The polish agent then drafted a detailed research spec for **0d.2** at
`docs/nightly-specs/2026-04-25-interaction-tests-signup-to-paid.md` —
explicitly so a build agent could pick it up tonight without a second
research pass. That's this run.

## What 0d.2 covers

The signup → trial → checkout flow is the only critical path that touches
money. A regression here ("Start trial does nothing", "Manage billing 500s")
silently loses customers, and nothing in the smoke sweep nor the unit suite
catches it today.

Implementation details have shifted since the parent 0d spec was written:

- The signup form does **not** redirect to Stripe Checkout for the default
  path. `app/routes/auth/signup.tsx` runs `ensureOrgForUser`, lands the org
  as `TRIALING` for 30 days, and redirects to the tenant board.
- `shouldStartCheckoutAfterSignup` only fires when the user **explicitly**
  picks a self-serve plan from `/pricing` (planSelectionSource === "explicit").
  When that fires, the action does call `createCheckoutSessionForOrg` and
  redirects to `https://checkout.stripe.com/...` directly.
- The independent Stripe entry point lives at `POST /api/billing/checkout`
  (admin Billing page + `/pricing` Form action).

So "signup → trial → checkout" is two redirects in two routes. The new spec
exercises both legs without driving Stripe's hosted UI.

## Files expected to touch

- **`e2e/flows/signup-to-paid.spec.ts`** (new, ~140 LOC) — two test cases:
  1. Default-path signup (no `?plan=` → defaults to CAR_LINE, lands on
     tenant board, `Org.status === "TRIALING"` in D1).
  2. `POST /api/billing/checkout` from a seeded-tenant admin returns a
     `302/303` to `https://checkout.stripe.com/...` and populates
     `Org.stripeCustomerId`. Conditional `test.fixme` if Stripe creds are
     absent in the runtime.
- `e2e/fixtures/seeded-tenant.ts` — small change only if needed: export a
  `teardownOrgById` helper so the trial-leg test can clean up its own org.
  If the spec can manage the cleanup inline cleanly, leave the fixture
  alone.
- `docs/nightly-queue.md` — flip 0d.2 to `[x]` with branch reference.
- `docs/nightly/2026-04-25-build.md` (this file).

No production code changes (`app/`, `workers/`, `prisma/` untouched) per the
research spec's "Files NOT touched" list.

## Acceptance criteria

- New `e2e/flows/signup-to-paid.spec.ts` exists.
- Trial-leg case is **green** when run against `wrangler dev` with default
  Stripe creds.
- Checkout-leg case is **green** when run with valid `STRIPE_*` creds in
  `.dev.vars`; `test.fixme(...)` with a clear message when not.
- `npm run typecheck` passes.
- `npm test` passes (unit suites unaffected, but verify).
- Spec deletes any rows it created on success **and** failure (per research
  spec's open question #5 — `dev.db` pollution would break next-run slug
  uniqueness on a re-run, even though parallel-spec isolation is already
  guaranteed by the random slug per spec).
- Build summary documents whether the checkout leg ran (i.e., whether Stripe
  was configured) and lists any bug surfaced per the queue's quality rule.

## Open questions answered up front

1. **`E2E_BYPASS_STRIPE`?** No — defer per research spec recommendation.
2. **Stripe staging creds?** Out of this run's scope; sandbox auth gap
   (recurring since 2026-04-21) means the staging-deploy + smoke gate is
   likely to skip again. Will document at gate time.
3. **Trial-leg seed-from-zero vs fixture?** Zero — the trial-leg drives the
   real signup UI (per research spec § "Why drive the real signup UI").
4. **CI Stripe secrets?** Out of scope; `e2e.yml` workflow not touched.
5. **Cleanup safety?** Use `test.afterEach` with try/catch around DELETEs.
   Same FK-ordered list as `seeded-tenant.ts § teardownSeedRows`.

## Gate plan

```bash
cd $WT
npm run typecheck
npm test
# Local Playwright sanity check before staging deploy:
npx playwright test e2e/flows/signup-to-paid.spec.ts --project=chromium
# Per AGENTS.md:
npm run d1:migrate:staging
npm run deploy:staging
PLAYWRIGHT_BASE_URL=https://school-organizer-staging.sundbergne.workers.dev \
  npx playwright test e2e/smoke.spec.ts --config=playwright.staging.config.ts
```

If wrangler auth is missing (recurring blocker since 2026-04-21), the run
ends BLOCKED, branch pushed for Noah's morning gate.

---

## Implementation log

(populated as commits land)

## Gate results

(populated after gate run)

## Outcome

(MERGED + commit range, or BLOCKED + reason)
