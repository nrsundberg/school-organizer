# Nightly polish — 2026-04-23

**Agent:** polish (nightly Pickup Roster agent).
**Window:** ~4 hours after nightly-build.

## Part A — Test coverage for tonight's build

**Skipped.** No `docs/nightly/2026-04-23-build.md` exists. The build
agent either didn't run tonight or is still running beyond the 2x
short-wait window I used in place of the prompt's 4×5min sleep (the
sandbox blocks long leading sleeps). The most recent build summary is
`docs/nightly/2026-04-22-build.md`, which documented workstream 0c
(`ci-playwright-matrix`) as **BLOCKED** — the 2026-04-22 branch exists
locally as `nightly-build/2026-04-22-0c-ci-playwright-matrix` but was
never pushed or merged (Host key verification failed, same `git push`
gap the 2026-04-21 run documented). No new user-visible behavior
landed on master today, so there's nothing to test.

Corroborating evidence that nothing landed tonight:

- `git log origin/master --since='6 hours ago'` returns nothing.
- No `nightly-build/2026-04-23-*` branch or worktree exists.
- No nightly-build-related commits on master since `2292187` from yesterday.

Recommendation for the morning: (a) fix the `git push origin` auth gap
that has now blocked three consecutive nightly runs from surfacing
work on GitHub (2026-04-21 0a, 2026-04-22 0c, any attempted 2026-04-23);
(b) review whether the build agent ran at all tonight and why — a
crash before writing the summary is indistinguishable from "didn't
run" given our current reporting.

## Part B — Research next queue item

**Written:** `docs/nightly-specs/2026-04-23-interaction-tests-critical-paths.md`
(0d, 270 lines).

Target workstream: **0d `interaction-tests-critical-paths`**, the next
Priority 0 queued item after 0c. The spec proposes a seeded-tenant
Playwright fixture under `e2e/fixtures/` plus five flow specs under
`e2e/flows/`:

- `signup-to-paid.spec.ts`
- `admin-roster.spec.ts`
- `dismissal.spec.ts`
- `viewer-pin.spec.ts`
- `branding-upgrade-gate.spec.ts`

Key design calls flagged for Noah:

1. **Stripe in CI.** The signup-to-paid spec stops at the
   `checkout.stripe.com/` redirect boundary rather than driving Stripe's
   hosted page. An optional E2E-only bypass in
   `billing/checkout.server.ts` is offered as an alternative for full
   trial→paid coverage.
2. **Admin session fixture.** The fixture inserts a Better Auth
   `Session` row directly rather than driving `/login` per test. Faster
   but couples tests to the Better Auth schema.
3. **Durable Object state across specs.** The dismissal spec mutates
   `BINGO_BOARD` DO state; the fixture needs an explicit
   `resetBoardForSpace()` helper or subsequent specs see stale ACTIVE
   state in `wrangler dev`.
4. **Viewer PIN lockout.** Fresh unique slug per spec avoids
   rate-limiter bleed between parallel runs.
5. **FREE plan seed shape.** Fixture writes `billingPlan: "CAR_LINE"`
   for the gated case and `"CAMPUS"` for the ungated case to match
   production paths rather than manufacturing a synthetic FREE state.

The spec is deliberately explicit about what stops the test at each
boundary and what remains on the build agent when it picks this up.

### Gate run

Per AGENTS.md, gated behind typecheck + unit tests (skipped staging
deploy per the prompt — no code changed). Both passed:

- `npm run typecheck`: clean after `react-router typegen` (required
  symlinking `app/db/generated/` from the main checkout into the
  worktree because `npx prisma generate` failed with `403 Forbidden`
  hitting `binaries.prisma.sh` — standard worktree gotcha documented
  in AGENTS.md "Prisma generated client").
- `npm test`: `102/102 tests pass` (56 subtests, 16 suites, ~730ms).

### Merge

Branch `nightly-research/2026-04-23` fast-forward-merged to `master`
as `6c0d467`. Not pushed to `origin` — same `git fetch/push` Host key
verification gap that's been blocking all agents this week.

## Blockers for a human

1. `git push origin` fails in sandbox ("Host key verification failed").
   All agent branches land locally only. Three consecutive nights of
   work (2026-04-21 0a, 2026-04-22 0c, 2026-04-23 0d research) are
   sitting on local branches that need to be pushed + PR'd manually.
2. `npx prisma generate` requires reachable
   `binaries.prisma.sh` — 403 Forbidden in sandbox. Worktrees inherit
   this gap. Symlinking `app/db/generated/` from the main checkout is
   the working-around-it pattern for now; worth a note in AGENTS.md.
