# Nightly polish — 2026-04-27

**Agent:** nightly-polish (scheduled).
**Status:** OK — research doc landed on master, no test work tonight.
**Branch (research):** `nightly-research/2026-04-27` (pushed to `origin`, merged to `master` via fast-forward; left in place per AGENTS.md until human cleanup).
**Master tip after this run:** `cfac5e6` (docs(nightly-specs): research spec for #5 data-export-delete).

## Run constraints

- bash sandbox: working. Per the standing 2026-04-26-polish open follow-up, the shared fuse mount at `/sessions/.../school-organizer/.git/refs/heads/nightly-build/` still has unkillable `.lock.dead-*` ref files. I did the entire research-worktree pass in a fresh `/tmp/so-polish-20260427-0611-*` clone and only the merge push touched origin (never the fuse mount's `.git/`). This pattern is now documented in `AGENTS.md § Defensive patterns` and is what every polish run should keep doing until a human shell can `rm` the dead refs.
- Auth: the SSH agent key at `<repo>/.git/agent_id_ed25519` (mounted in the fuse repo's `.git/`) works for github.com pushes via `GIT_SSH_COMMAND`. No PAT needed.
- Additional fuse-mount cruft observed: `.git/refs/heads/agent-fix/2026-04-27-healthz-public-allowlist.lock.dead-*` (a fresh `.dead-` lockfile from another concurrent run). Did NOT touch the fuse `.git/` directly to avoid the same trap; instead worked entirely off the /tmp clone.

## Part A — test coverage for tonight's build

**Skipped.** `docs/nightly/2026-04-27-build.md` did not exist after the polling window, no `nightly-build/2026-04-27*` branch on origin, and no commits on `origin/master` newer than the morning scanner's `a74ddbb1` (which is itself a docs update, not feature code). Per the prompt's branching rule:

> If `$SUMMARY` doesn't exist after waiting: build never ran or is still going. Skip Part A, go to Part B.

This is the third polish run in a row where build didn't fire (2026-04-25, 2026-04-26, 2026-04-27 are all polish-only nights — see those summaries). The build agent's scheduling has been intermittent for almost a week; flagging here because the queue is now visibly piling up uncovered work that polish-only nights can't drain. Recommend Noah look at the build agent's schedule cadence — at this rate the unblocked items in `docs/nightly-queue.md` are growing faster than they're shipping. (The 0d.1, 0d.2, 0d.3 branches are all already pushed-for-review from earlier nights and waiting on the staging gate Noah needs to manually run; that's an additional, separate, not-polish-fixable bottleneck.)

## Part B — research next queue item

**Picked:** Priority 2, item **#5 — `data-export-delete`** in `docs/nightly-queue.md`. Selection rationale:

- 0d-prereq is `[ ]` but flagged "effectively done" by the standing notes; the fixture it would land has been in production use since 2026-04-23-manual-1438.
- 0d.1, 0d.2, 0d.3 all already have research specs landed AND build branches pushed — they're waiting on Noah's gate, not on more research.
- Priority 1 #1 `roster-csv-import` and Priority 1 #3 `support-contact` already have full research specs (`docs/nightly-specs/2026-04-23-manual-1438-roster-csv-import.md`, `docs/nightly-specs/2026-04-24-support-contact.md`) and were largely shipped — `roster-import.tsx` is on master, contact form is on master.
- Priority 1 #2 `legal-pages` is `[!]` (blocked on Noah's legal inputs).
- Priority 2 #5 `data-export-delete` is the highest-priority `[ ]` item with no existing research spec and no in-flight build branch. It's two routes plus a sidebar/i18n hook, well within a single-night build budget.
- Priority 2 items #6–#11 and Priority 3+ all sit downstream of this one; tackling out-of-priority would be against the queue's stated rule of "next highest priority."

**Doc landed:** `docs/nightly-specs/2026-04-27-data-export-delete.md` (302 lines).

**Spec covers:**

- `/admin/data-export` — streamed-zip via `fflate` (recommended over jszip/archiver/hand-rolled, with rationale), gated by `planAllowsReports` (existing helper), audited via `recordOrgAudit` (existing helper) with new action `data.export`. Includes per-table whitelist of columns to dump (with explicit drop-list for `passwordHash`, `viewerPinHash`, Stripe IDs, etc.), pagination strategy for D1's 1 MB-per-query limit, and a manifest.json shape.
- `/admin/data-delete` — destructive double-confirm flow (type the slug + check an "irreversible" box). Walks the Org cascade explicitly via `prisma.$transaction` with a 15-step delete order that respects the FK chain, deliberately preserves the `Org` row + Stripe customer/subscription columns + `OrgAuditLog` history. New audit action `data.delete_all` records `rowCountsBefore` for forensic durability.
- Sidebar + i18n: `Download` icon entry under `/admin/data-export`; `data-delete` is intentionally NOT in the sidebar (surfaced from inside the export page as a "Danger zone" footer card, GitHub-repo-settings style).
- Tests: unit suites for whitelist + delete-order, Playwright spec with three cases (CAR_LINE upsell, CAMPUS happy-path zip, CAMPUS happy-path delete with Org-row-survives assertion), smoke-sweep entry for `/admin/data-export` only.
- File list (6 new, 6 modified, none-touched callout).
- Six open questions resolved with recommendations: include `OrgAuditLog` in export but not delete (#1); skip executing admin's User row, two-step cleanup (#2 Option A); whitelist columns enumerated per table (#3); rate-limit at 1/5min/org (#4); board DO cache invalidation punted to follow-up #5.1 (#5); R2 logo cleanup as best-effort post-txn (#6).

## Gate

Per AGENTS.md, docs-only changes skip the staging deploy step. The remaining gate items, all run inside the /tmp clone:

| Step | Result | Notes |
|---|---|---|
| `npm ci --prefer-offline` | OK | Clean install. Pre-existing audit warnings unchanged. |
| `npx prisma generate` | OK | 132 ms; needed because `app/db/generated/` is gitignored. |
| `npm run typecheck` (`react-router typegen && tsc`) | **OK** | Zero errors. The pre-existing typecheck breakage flagged in 2026-04-26-polish has been resolved on master since then — confirmed clean run on `cfac5e6`. |
| `npm test` (Node `--test`) | OK | 250 tests in 31 suites, all green. ~4.4 s. |
| Staging deploy | SKIPPED | Per task spec: "skip staging deploy since no code changed." |

## Merge

Fast-forward to master:

```
a74ddbb..cfac5e6  master -> master
```

`nightly-research/2026-04-27` pushed to `origin` and merged into `master` via `--ff-only`. Both are now at `cfac5e6`.

The branch is **not** deleted from origin per the same convention 2026-04-26-polish established (cheap to clean up later from any shell, useful as a single-doc PR view if Noah wants one).

## Open follow-ups

- **Build agent scheduling.** Three polish-only nights in a row (2026-04-25, 2026-04-26, 2026-04-27 — last polish summary was `BLOCKED` on staging, two polish summaries before that were `Skipped Part A — build did not run`). Worth a once-over on the cron / scheduling layer that fires the build agent.
- **Stale fuse-mount ref files (carry-over).** Two unkillable `.lock.dead-*` files in `.git/refs/heads/nightly-build/` from a prior aborted run, plus a third in `.git/refs/heads/agent-fix/` from this morning's scanner run. The /tmp-clone workaround handles it for scheduled tasks, but a human-shell `rm` would let scheduled tasks use the fuse mount directly again. Files of note this run:
  - `.git/refs/heads/nightly-build/2026-04-25-0d2-signup-to-paid.lock.dead-*` (×2 — already noted in 2026-04-26-polish)
  - `.git/refs/heads/agent-fix/2026-04-27-healthz-public-allowlist.lock.dead-*` (new this run)
- **Pending PRs awaiting Noah's staging gate (carry-over from earlier nights).** `nightly-build/2026-04-24-0d1-dismissal`, `nightly-build/2026-04-25-0d2-signup-to-paid`, `nightly-build/2026-04-26` all remain pushed-but-not-merged. These tonight's research spec doesn't change anything for them; just flagging that the queue's Priority 0 list looks like more `[ ]` items than reality, since most of those branches are already half-shipped.

## Summary line

Part A: skipped (build did not run tonight, no summary). Part B: research spec written for #5 data-export-delete, merged to master.
