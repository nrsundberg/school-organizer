# Nightly polish — 2026-04-28

**Status:** PARTIAL — Part A skipped (build BLOCKED on staging gate, didn't land on master). Part B shipped: research spec for queue item #6 (`uptime-monitor`) merged to master.
**Agent:** nightly-polish (scheduled).
**Companion to:** `docs/nightly/2026-04-28-build.md`.

## Part A — Test coverage for tonight's build

**Skipped.** Per the polish-agent runbook: when the build summary contains "BLOCKED" (i.e., the build's branch was pushed but didn't merge to master), Part A is intentionally a no-op. Adding tests for code that lives on an unmerged branch would either:

1. Be written against `master` and break the moment Noah's morning gate merges `nightly-build/2026-04-28`, or
2. Be written on top of the unmerged branch, doubling the merge-conflict surface area Noah has to navigate.

Both are anti-patterns. The cleaner path: when Noah's morning gate merges `nightly-build/2026-04-28`, the next nightly-polish run picks up where this one left off — the research spec for `data-export-delete` already explicitly recommends three Playwright cases (CAR_LINE upsell, CAMPUS happy-path zip, CAMPUS delete preserves Org row), and the build summary's "Out of scope tonight" section flags them as a follow-up gated on `nightly-build/2026-04-26` landing first. Both branches in flight are independent, so either can land first; whichever does, the next polish agent can write the spec's Playwright cases against it.

The build's status was confirmed by reading `docs/nightly/2026-04-28-build.md`:

> **Status:** BLOCKED — staging gate; branch pushed for Noah's morning review.

with the staging deploy + smoke skipped due to the recurring `CLOUDFLARE_API_TOKEN` sandbox-auth gap (7th run in a row blocked on the same issue). The branch passes typecheck + 342 unit tests locally; only the external staging leg is unverified.

## Part B — Research for next queue item

**Item picked:** Priority 2, item **#6 — `uptime-monitor`** in `docs/nightly-queue.md`.

### Why this item

Walking the queue from the top:

- **0d-prereq** (`seeded-tenant-e2e-harness`) — still listed `[ ]` but its substance landed 2026-04-23; the polish agent's standing note flags it as effectively done. Skip.
- **0d.1 / 0d.2 / 0d.3** — all three have build branches pushed and waiting on Noah's morning gate. No agent value in re-research.
- **#1 `roster-csv-import`** — has a complete spec at `docs/nightly-specs/2026-04-23-manual-1438-roster-csv-import.md`, build-ready.
- **#2 `legal-pages`** — `[!]` blocked pending Noah's inputs.
- **#3 `support-contact`** — has a complete spec at `docs/nightly-specs/2026-04-24-support-contact.md`, build-ready.
- **#4 `ops-runbook`** — `[x]` done.
- **#5 `data-export-delete`** — tonight's build target, branch in flight.
- **#6 `uptime-monitor`** — the next priority item with **no spec yet** and queue scope that explicitly says "research + config, not code." This is the perfect polish-agent shape: the spec IS the deliverable, not a brief for a future build.

The next polish agent (or build agent) won't run out of build-ready specs after this run — `#1`, `#3`, and the three Priority-0 sub-items all still have specs awaiting build. Tonight's research closes the next gap one rung down.

### What was written

`docs/nightly-specs/2026-04-28-uptime-monitor.md` (213 lines), covering:

- **Build vs. buy** — recommend buy. Self-hosting on the same provider as the thing being monitored fails for the same reason its target does, exactly when needed most.
- **Provider comparison matrix** — UptimeRobot vs. BetterStack vs. Cronitor across check interval, free monitor count, notification channels, on-call routing, hosting cloud, and notable caveats.
- **Recommendation: BetterStack free tier.** 3-min interval (vs. UptimeRobot's 5-min — closes the dismissal-window detection gap), free phone/SMS, on-call rotations, hosted off-Cloudflare. Free tier covers our 7-monitor draft with 3 monitors of headroom; upgrade trigger pinned at "pilot #3 onboarded with their own subdomain monitor."
- **7 pre-drafted monitors** — marketing apex, `/api/healthz`, `/login`, `/status`, staging healthz, workers.dev fallback healthz, one demo tenant subdomain. Each row spec'd with method, expected status, keyword-match assertion, interval, and rationale (which class of fault each catches).
- **Three-channel alert routing** — Slack `#alerts-pickuproster` primary (loud, free, mobile push), email to noahsundberg@gmail.com + support@pickuproster.com secondary, phone call after 10 min unacknowledged tertiary. Acknowledge flow + auto-resolve documented.
- **`docs/incidents/` integration** — alert payload includes a one-line pointer to ops-runbook § "Post-incident" so the first real outage spawns the markdown file the runbook already references.
- **File list** — small. `docs/uptime-monitor.md` (post-buy summary) recommended but flagged as Noah-writes-after-account-creation; `docs/incidents/.gitkeep` to pre-create the directory; one-line edit to `docs/ops-runbook.md` § "Communication tree" item 3 to replace the placeholder pointer with a concrete reference.
- **Testing approach** — purely manual, since external monitoring is by definition out-of-band. Spec'd as a 15-min synthetic-outage exercise: point a monitor at a 404, watch Slack + email fire, acknowledge, revert, watch auto-recover.
- **6 open questions** — demo-tenant timing (swap, don't wait), Slack channel naming, public status page (deferred to first-pilot-conversation trigger), Sentry alert dedup (route Sentry alerts to same Slack channel), Cronitor for cron monitoring (separate follow-up workstream), multi-region probes (deferred until first regional-asymmetry incident).

The spec ends with a queue-summary line so the build agent doesn't have to re-summarize.

### Gate result

Run from a `/tmp` clone (per AGENTS.md "Defensive patterns" § 2 — bypasses the fuse mount's `unlink()` block on `.git/` lock files):

| Step | Result |
|---|---|
| `npm ci --prefer-offline --no-audit --no-fund --ignore-scripts` | OK (1025 pkgs in 6 s) |
| `npx prisma generate` | OK |
| `npm run typecheck` (`react-router typegen && tsc`) | OK (clean) |
| `npm test` | OK (335 / 335 tests, 52 suites, 0 fail, 4.5 s) |
| Staging deploy + smoke | Skipped — docs-only diff, per polish-agent runbook. |

Branch `nightly-research/2026-04-28` (commit `f6138ad`) merged via fast-forward to `master`, pushed to `origin`. No conflicts with master tip (`089c1d5`).

### Files changed (on master)

```
 docs/nightly-specs/2026-04-28-uptime-monitor.md | 213 ++++++++++++++ (new)
```

## Blockers

- **Recurring sandbox-auth gap** — note that this run successfully bypassed the wrangler-auth issue by being docs-only. Tonight's build was the 7th consecutive nightly-build run blocked on `CLOUDFLARE_API_TOKEN` not being available in the sandbox. The polish agent's standing recommendation: implement option (b) from the build summary — a "staging-safe" toggle for diffs that only touch `app/`, `e2e/`, `docs/`, `tests/` (no infra), so the build can finish its gate even when wrangler isn't authenticated.

## Summary line

> nightly-polish 2026-04-28: Part A skipped (build #5 BLOCKED on staging gate; tests for unmerged code is an anti-pattern). Part B written and merged to master — `docs/nightly-specs/2026-04-28-uptime-monitor.md` (213 lines): pick BetterStack free tier, pre-draft 7 external monitors with keyword-match assertions, three-channel alert routing (Slack primary, email secondary, phone tertiary). Closes the research half of queue item #6.
