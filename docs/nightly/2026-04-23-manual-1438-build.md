# Nightly build — 2026-04-23-manual-1438 (0d interaction-tests-critical-paths, partial)

**Agent:** nightly-build (manual one-off run at ~14:38 EDT).
**Branch:** `nightly-build/2026-04-23-manual-1438`.
**Worktree:** `.worktrees/2026-04-23-manual-1438`.
**Queue item picked:** Priority 0 item **0d — interaction-tests-critical-paths** (top unblocked entry in `docs/nightly-queue.md`).
**Research prior:** `docs/nightly-specs/2026-04-23-interaction-tests-critical-paths.md` (polish agent, earlier today). That spec flagged five open questions for Noah that have not been answered yet. Per the manual-run rules, this build makes the spec's recommended defaults rather than waiting for input — see "Defaults applied" below.

## Plan

The research spec proposes a `seeded-tenant` Playwright fixture + five flow specs under `e2e/flows/`. Full delivery in one night is not realistic (especially without Noah's call on the open questions and without a working staging gate). This build ships the **foundation** so the rest can land in follow-up runs:

1. Extract password hashing + id generation from `scripts/seed.ts` into `e2e/fixtures/seed-helpers.ts` so the fixture and the seed script share the same PBKDF2 code path.
2. Update `scripts/seed.ts` to import from the new helpers (lift-and-shift, no behavior change).
3. Add `e2e/fixtures/seeded-tenant.ts` — Playwright `test.extend()` fixture that inserts a per-spec Org + admin User + Session + AppSettings (with hashed viewer PIN) + HomeRoom + Space against `file:./dev.db`, and returns `{ slug, adminCookie, viewerPin, spaceNumber, homeroomId, userId, orgId }`.
4. Add two flow specs that stand on the fixture:
   - `e2e/flows/admin-roster.spec.ts` — admin creates homeroom + student + assigns space, asserts roster reflects it.
   - `e2e/flows/viewer-pin.spec.ts` — wrong PIN rate-limits, correct PIN sets viewer-session cookie.
5. Gate `e2e/flows/**` off the mobile Playwright projects via `testIgnore` so CI time doesn't double (already prescribed by the research spec).
6. Follow-up items (`signup-to-paid`, `dismissal`, `branding-upgrade-gate`) are appended to `docs/nightly-queue.md` with pointers to the fixture so a later build agent can pick each up independently.

### Defaults applied for the five open questions

1. **Stripe Checkout in CI** — stop at the `checkout.stripe.com` redirect. No E2E bypass code in the billing layer.
2. **Admin session** — insert a `Session` row directly, return the `better-auth.session_token` cookie value. Accepts schema coupling to Better Auth as the tradeoff for speed + determinism.
3. **Durable Object state** — the dismissal spec (not shipped this run) will need an explicit `resetBoardForSpace` helper. Noted in the follow-up queue item.
4. **Viewer-PIN rate-limit** — use unique slugs per spec so the RL Durable Object state can't cross-contaminate. Default in the fixture.
5. **FREE plan seeding** — fixture accepts `billingPlan: "CAR_LINE" | "CAMPUS" | "FREE"` and writes it directly.

### Acceptance criteria

- `npm run typecheck` clean on the branch.
- `npm test` clean on the branch (unit tests only — the new code is e2e, not unit).
- The new fixture + helpers compile and the two flow specs typecheck.
- Actual flow-spec execution is NOT part of this run's gate because `wrangler dev` in the Cowork fuse sandbox has documented `SQLITE_IOERR_DELETE` flakiness (see `docs/nightly/2026-04-21-build.md`) and the staging gate has been marked unavailable for the last three nights. A follow-up run on a non-fuse host can actually execute the specs.

### Files touched

- **new** `e2e/fixtures/seed-helpers.ts`
- **new** `e2e/fixtures/seeded-tenant.ts`
- **new** `e2e/flows/admin-roster.spec.ts`
- **new** `e2e/flows/viewer-pin.spec.ts`
- **edit** `scripts/seed.ts` — import helpers
- **edit** `playwright.config.ts` — add `testIgnore: /flows\//` to mobile projects
- **edit** `docs/nightly-queue.md` — split 0d into shipped + three follow-up items

## Status

**BLOCKED — staging gate unavailable.** Same state the 0c build hit on 2026-04-22 (`docs/nightly/2026-04-22-build.md`): wrangler in this sandbox reports "You are not authenticated. Please run `wrangler login`." so `npm run d1:migrate:staging` and `npm run deploy:staging` cannot run, and the Playwright staging smoke check therefore can't be exercised. This is a Noah-action (human wrangler login) and is out of scope for an autonomous agent.

Branch pushed to `origin/nightly-build/2026-04-23-manual-1438`. `gh` is not installed in this sandbox so a PR was not opened — Noah can open a draft PR manually from the push hint GitHub returned:

    https://github.com/nrsundberg/school-organizer/pull/new/nightly-build/2026-04-23-manual-1438

### Gate results

| Gate | Result |
| --- | --- |
| `npm run typecheck` | **PASS** — clean after `npx prisma generate` (gitignored dir, per-worktree) and clearing stale `.react-router/types/*` via mv-not-rm (fuse mount). |
| `npm test` | **PASS** — 102/102, 16 suites, 914ms. |
| `npm run d1:migrate:staging` | **BLOCKED** — wrangler not authenticated. |
| `npm run deploy:staging` | **BLOCKED** — wrangler not authenticated. |
| Playwright staging smoke (`e2e/smoke.spec.ts` against staging) | **BLOCKED** — no staging deploy to point at. |
| Local Playwright flow run on `wrangler dev` | **NOT ATTEMPTED** — documented SQLITE_IOERR_DELETE flake inside the fuse sandbox (see `docs/nightly/2026-04-21-build.md`). The two new flow specs compile and typecheck, but end-to-end validation needs a non-fuse host. |

### Commits

- `cd5277e wip: 0d foundation — seeded-tenant fixture + two flow specs`

### Follow-ups

- 0d in the queue flipped from `[ ]` to `[→]` with progress notes.
- Three new unblocked items appended: 0d.1 (dismissal spec), 0d.2 (signup-to-paid spec), 0d.3 (branding-upgrade-gate spec). Each depends on the fixture that landed tonight.
- The staging-gate unavailability has now blocked three consecutive build runs (0c on 2026-04-22, this run's 0d foundation, and the 0c revisit on 2026-04-23 per the master log). Noah running `wrangler login` once would unblock every subsequent nightly build agent.

### Bugs found during testing

None. The two new flow specs were not executed end-to-end this run, so they have not had the chance to uncover real-behavior regressions. That happens on the first successful staging+wrangler-dev run.

### Defaults applied (for the research spec's open questions)

1. Stripe Checkout in CI — stop at the `checkout.stripe.com` redirect boundary.
2. Admin session — `Session` row insert + cookie, name `tome.session_token`.
3. DO board reset — noted as required work in the 0d.1 follow-up.
4. Viewer-PIN rate-limit — unique slug per spec.
5. FREE plan seeding — fixture accepts `billingPlan: "FREE" | "CAR_LINE" | "CAMPUS"` via `testInfo.project.metadata.tenantBillingPlan`.
