# Nightly polish — 2026-04-24

**Agent:** nightly-polish (scheduled).
**Base:** `origin/master` @ `73db055` after this run (was `5e08d31`
when polish started; `94efab6` when the build summary was written).

## Part A — Test coverage for tonight's build

**Skipped.** Tonight's build (`docs/nightly/2026-04-24-build.md`) is
**BLOCKED** — the wrangler staging gate couldn't run in the sandbox
(unauthenticated), so the branch `nightly-build/2026-04-24` was
pushed for Noah's morning review and **did not merge to master**.
Per the polish prompt: "If `$SUMMARY` exists AND contains BLOCKED:
skip Part A — the build didn't land."

The build was docs-only anyway (workstream 4 `ops-runbook`:
`docs/ops-runbook.md` + `docs/dismissal-day-checklist.md`), so there
is no runtime behavior that would benefit from new Playwright
coverage at this point — if/when Noah merges the build branch
tomorrow, no test-coverage backfill is needed.

## Part B — Research for the next queue item

**Workstream:** Priority 1, item 3 — `support-contact` (Contact
form + footer/nav link).

**Picked because:** item #4 `ops-runbook` was taken by tonight's
build. Items ahead of it in priority were either already covered or
unpickable:

- #1 `roster-csv-import` — already has a thorough spec at
  `docs/nightly-specs/2026-04-23-manual-1438-roster-csv-import.md`
  (manual-1438 polish run). Re-researching it would have been
  wasted effort.
- #2 `legal-pages` — `[!]` blocked pending Noah's legal-entity
  inputs.
- 0d subtasks (0d.1 / 0d.2 / 0d.3) — blocked on the seeded-tenant
  harness per tonight's build summary.

That leaves #3 `support-contact` as the next queued, unblocked item
without a prior spec.

**Deliverable:** `docs/nightly-specs/2026-04-24-support-contact.md`
(405 lines). Covers:

- Problem framing (no capture surface today; mailto-only).
- Current-state map: `getSupportEmail` helper in `app/lib/site.ts`,
  the existing `EmailMessage` / `enqueueEmail` / `sendEmail` queue
  pipeline in `app/domain/email/`, the `RL_AUTH` / `RL_BILLING`
  rate-limit binding pattern in `wrangler.jsonc` +
  `app/domain/utils/rate-limit.server.ts`, `zfd.formData` form
  pattern, `isMarketingHost` / `marketingOriginFromRequest` host
  gating, no existing spam protection.
- Proposal: one new public route `app/routes/contact.tsx`; new
  `ContactFormMessage` kind + template wired through the existing
  queue (so a Resend outage doesn't surface to the submitter); new
  `RL_CONTACT` binding (1/IP/60s prod, 5/60s staging); honeypot-only
  spam protection for PR 1, Turnstile deferred.
- File list: 13 files (5 new code files, 2 new tests, 6 modifications
  across `routes.ts`, `Footer.tsx`, `MarketingNav.tsx`,
  `wrangler.jsonc`, `docs/rate-limiting.md`, and `e2e/smoke-routes.ts`).
  Zero Prisma schema changes, zero new dependencies.
- Testing approach: unit tests for the action (mocks for
  `EMAIL_QUEUE` + `RL_CONTACT`), renderer snapshots, one e2e spec,
  plus a smoke entry.
- Seven open questions flagged for Noah (Turnstile timing,
  DB-persistence of submissions, per-topic routing, auto-reply to
  sender, nav density, char limits, a11y).

## Gate

| Step | Result | Notes |
|---|---|---|
| `npm run typecheck` | PASS | 0 errors. Ran after `npx prisma generate` + a fresh `react-router typegen` (auto-run by the typecheck script). |
| `npm test` | PASS | 102 pass / 0 fail across 16 suites. |
| `npm run d1:migrate:staging` | SKIPPED | Docs-only diff; polish prompt says skip staging for research. |
| `npm run deploy:staging` | SKIPPED | Same. |
| Staging Playwright smoke | SKIPPED | Same. |

## Merge

Merged to `origin/master` via plumbing (build a new commit onto a
fresh `origin/master` tree and push the SHA as `refs/heads/master`)
because the sandbox hit a cascade of stale `.lock` files inside
`.git/worktrees/…` that the fuse mount couldn't unlink and git
wouldn't ignore. Net result is a clean fast-forward:

```
5e08d31..73db055  73db055 -> master
```

Commit `73db055 docs(research): 2026-04-24 support-contact spec
(workstream #3)`. One file added:
`docs/nightly-specs/2026-04-24-support-contact.md`.

Temporary review branch `nightly-research/2026-04-24-support-contact-v2`
was pushed as a backup before the master FF and remains on origin
(local cleanup of remote-tracking refs failed on the fuse-mount
EPERM — not a blocker; GitHub will show the branch as merged).

## Follow-ups

- Ops-runbook build branch `nightly-build/2026-04-24` still needs
  Noah's morning gate run + merge (steps in
  `docs/nightly/2026-04-24-build.md § Follow-ups for Noah`).
- Tomorrow's build can pull `support-contact` off the queue: the
  spec is detailed enough that a build agent can go straight to
  coding without a second research pass.
- The `.git/*.lock.dead-*` pollution inside `.git/refs/` on the main
  checkout is still accumulating across runs because the fuse mount
  blocks `rm`. It occasionally bricks `git fetch` (hit once tonight
  — had to mv the offending files to `.git/trash/` before fetch
  would work). Worth adding to the known-gotchas section of
  `AGENTS.md` or writing a one-shot cleanup script.
