# Nightly polish — 2026-04-25

**Agent:** nightly-polish (scheduled).
**Status:** DEGRADED — sandbox shell unavailable for the entire run; no
git operations (no worktree, no commit, no push, no merge) and no gate
runs (no `npm`, no `npx`, no `wrangler`). File-tool writes landed
directly in the master checkout's working tree. Noah will see the new
files as untracked when he opens the repo and must commit + push them
manually.

## What broke

Every `mcp__workspace__bash` call returned, immediately and on every
retry:

```
RPC error: ensure user: useradd failed: exit status 1:
  useradd: /etc/passwd.<n>: No space left on device
  useradd: cannot lock /etc/passwd; try again later.
```

The sandbox host's root filesystem is full and `useradd` (run when the
shell session is provisioned) cannot write to `/etc/passwd`. Without
the shell, every step in the polish prompt that requires git, npm, or
wrangler is unrunnable. File tools (Read / Write / Edit / Glob / Grep)
worked normally because they go through a different transport.

## Part A — Test coverage for tonight's build

**Skipped, two reasons:**

1. **No build summary exists for tonight.** `docs/nightly/2026-04-25-build.md`
   was not present at the start of this run. The polish prompt's wait
   loop (4 × 5 min) couldn't be honored — without `bash` there's no
   `sleep`, but the underlying issue (no shell at all) means the build
   couldn't have run either. Per the prompt: *"If `$SUMMARY` doesn't
   exist after waiting: build never ran or is still going. Skip Part A,
   go to Part B."*
2. **Even if a build summary had landed, the polish gate is unrunnable
   in this sandbox.** Adding tests would imply being able to run them
   (`npm test`, `npx playwright test`, staging deploy + smoke). None of
   those work. Pushing a tests-only branch with no gate evidence would
   violate AGENTS.md.

The most recent build (`docs/nightly/2026-04-24-0d1-dismissal-build.md`,
0d.1 dismissal flow) is **BLOCKED** awaiting Noah's morning gate +
merge — it pushed `nightly-build/2026-04-24-0d1-dismissal` to origin
with three passing cases plus one `test.fixme` flagging a real DO bug
(`workers/bingo-board.ts` writes `CallEvent` with no `orgId`, which
prevents non-`org_tome` tenants from seeing their own dismissal events
on `/admin/history`). That branch's tests are already adequate
coverage for what 0d.1 changed, so even on a working sandbox there
would be little for tonight's polish to add on top.

## Part B — Research for the next queue item

**Workstream:** Priority 0, item **0d.2 — `interaction-tests-signup-to-paid`**
(Playwright e2e for signup → trial → Stripe Checkout redirect).

**Picked because:** it is the strict top-of-queue unblocked `[ ]` item
without an existing spec.

- 0d-prereq is effectively done (`e2e/fixtures/seeded-tenant.ts` exists
  on master; the 04-24 build's queue update inserting it as a `[ ]`
  hasn't merged yet but the substance landed weeks ago).
- 0d.1 just had a build last night, branch awaiting Noah.
- The next is 0d.2 (signup-to-paid), then 0d.3 (branding-gate).
- Items above 0d in priority that are queued: P1 #1 roster-csv-import
  (spec already at `docs/nightly-specs/2026-04-23-manual-1438-roster-csv-import.md`),
  P1 #2 legal-pages (`[!]` blocked), P1 #3 support-contact (spec at
  `docs/nightly-specs/2026-04-24-support-contact.md`), P1 #4 ops-runbook
  (`[x]` done). Nothing else without a spec.

**Deliverable:** `docs/nightly-specs/2026-04-25-interaction-tests-signup-to-paid.md`.

The spec covers:

- The mismatch between the queue's original framing ("signup redirects
  to checkout.stripe.com") and the actual implementation (signup ends
  at the tenant board with `Org.status = TRIALING`; Stripe entry is
  later, via `POST /api/billing/checkout` from the admin Billing page
  or /pricing).
- Two-test design: (a) drive the real 3-step signup UI on the marketing
  host, assert the tenant-board redirect + `Org.status = TRIALING` in
  D1; (b) on the seeded-tenant fixture, `POST /api/billing/checkout`
  with `maxRedirects: 0` and assert the `Location` header begins with
  `https://checkout.stripe.com/`.
- Recommendation on the queue's open question #1 (`E2E_BYPASS_STRIPE`):
  **defer**. A bypass flag wouldn't catch the regressions we actually
  fear (renamed price IDs, wrong redirect destination); both surface
  from the real call. Build agent gets a small implementation sketch
  if Noah wants to override.
- Conditional `test.fixme` for the checkout leg when Stripe creds are
  absent in `.dev.vars` — keeps the spec runnable on Noah's local
  machine without papering over the gap.
- File list: 1 new spec file (`e2e/flows/signup-to-paid.spec.ts`,
  ~140 LOC), one optional fixture refactor extracting an exported
  `teardownOrgById` helper. Zero production-code changes.
- Acceptance criteria + 5 open questions for the build agent
  (E2E_BYPASS_STRIPE decision, staging Stripe creds verification,
  fixture-vs-zero state for the trial leg, CI Stripe secrets,
  cleanup-on-crash safety).

## Gate

| Step | Result | Notes |
|---|---|---|
| `npm run typecheck` | NOT RUN | bash sandbox down — `useradd` EFS full. |
| `npm test` | NOT RUN | same. |
| `npm run d1:migrate:staging` | SKIPPED | docs-only diff per polish prompt. |
| `npm run deploy:staging` | SKIPPED | same. |
| Staging Playwright smoke | SKIPPED | same. |

The new spec file is docs-only and contains zero TS/JS, so a typecheck
pass is mechanically guaranteed when bash returns. Noah can run
`npm run typecheck && npm test` locally to confirm.

## Merge

**NOT MERGED.** No worktree was created (no `git worktree add`
possible without bash). Files were written directly to the master
working tree at `/Users/noah/personal/dev/school-organizer/`. They
will appear as untracked in `git status`:

- `docs/nightly-specs/2026-04-25-interaction-tests-signup-to-paid.md`
- `docs/nightly/2026-04-25-polish.md` (this file)

This is the same fallback `docs/nightly-queue.md` documents for
git-auth failures: *"If `gh` or `git push` auth fails, keep the branch
local and flag it in the summary — Noah can push/PR manually in the
morning."* The branch in question here doesn't exist at all, but the
file artifacts do, and Noah can land them with:

```bash
cd /Users/noah/personal/dev/school-organizer
git checkout -b nightly-research/2026-04-25-signup-to-paid
git add docs/nightly-specs/2026-04-25-interaction-tests-signup-to-paid.md \
        docs/nightly/2026-04-25-polish.md
git commit -m "docs(research): 2026-04-25 signup-to-paid spec (workstream 0d.2)"
# Optional gate (docs-only, so typecheck + unit are the relevant ones):
npm run typecheck && npm test
git push -u origin nightly-research/2026-04-25-signup-to-paid
git checkout master
git merge --ff-only nightly-research/2026-04-25-signup-to-paid
git push origin master
```

## Follow-ups

1. **Sandbox disk full.** Repeated runs are going to keep failing this
   way until the host's `/` is reclaimed. This isn't a repo-side fix —
   it's a Cowork session image / VM provisioning issue. Worth a
   one-line note to whoever runs the scheduled-task infra.
2. **0d.1 dismissal branch still needs Noah's morning gate run + merge**
   per `docs/nightly/2026-04-24-0d1-dismissal-build.md § Next actions
   for Noah`. Includes the `dismissal-do-tenant-scoping` bug suggested
   as a new queue entry — file when convenient.
3. **Ops-runbook branch (`nightly-build/2026-04-24`) still awaiting
   Noah's gate + merge** per `docs/nightly/2026-04-24-build.md §
   Follow-ups for Noah`. Same wrangler-auth blocker as every build
   since 2026-04-21.
4. **Tomorrow's build can pick up `signup-to-paid` directly** — the
   spec is detailed enough that a build agent can go straight to coding
   without a second research pass.
5. **Recurring sandbox-auth gap for nightly builds** is now four runs
   old (2026-04-21, 2026-04-22-0c, 2026-04-24 ops-runbook, 2026-04-24
   0d.1). Worth a queue item: cache wrangler creds in the session
   image, OR add a docs-only "staging-safe" toggle that bypasses the
   staging gate when no production code changed, OR formalize that
   nightly-BLOCKED is acceptable as long as the morning gate runs.
