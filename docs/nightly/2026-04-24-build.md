# Nightly build — 2026-04-24

**Status:** IN PROGRESS
**Agent:** nightly-build (scheduled).
**Workstream:** 4 — `ops-runbook` (pilot-blocker, P1, docs-only).
**Branch:** `nightly-build/2026-04-24`.
**Base:** `origin/master` @ `e8f3d72` (merge: nightly-scanner/2026-04-23-1439-manual-1438).

## Why not the strict top-of-queue item (0d)?

Strict "top unblocked" is **0d — interaction-tests-critical-paths**
(`e2e/flows/*.spec.ts` for signup-to-paid, admin-roster, dismissal,
viewer-pin, branding-upgrade-gate). The current smoke sweep
(`e2e/smoke.spec.ts`, landed 2026-04-21) explicitly documents why those
flows are not yet runnable in the existing wrangler-dev harness:

> Seeded authenticated coverage (a real admin on a real tenant subdomain,
> and a real PLATFORM_ADMIN on the platform host) is out of scope for this
> smoke sweep. It requires:
>   - A tenant-slug host override (wrangler dev serves localhost only; dev
>     subdomains like `smoke-<ts>.localhost` aren't reachable from the
>     Playwright baseURL without /etc/hosts or a proxy).
>   - A seeded tenant with billing bypassed (signup ends at Stripe checkout
>     in the real flow).

Standing up that harness (Host-header proxy in the Playwright fixture +
billing-bypass seed) is itself a multi-night workstream. Shipping partial
flow specs where most tests are `.fixme`'d on the same blocker would be
worse than nothing — the queue's own quality rule says "Codifying a bug as
'passing' is worse than no test."

Workstream 4 (ops-runbook) is the next pilot-blocker on the P1 list, is
docs-only (zero risk of breaking the gate), and delivers the kind of
artifact a school admin can print and tape to the front desk on day one.
A follow-up queue item is added for the 0d seeded-flow harness.

## Plan

Two docs, both in `docs/`:

1. **`docs/ops-runbook.md`** — on-call runbook. Covers pre-deploy
   checklist, rollback via `wrangler rollback`, how to force-failover to
   the print master list (`/admin/print/master`), on-call escalation
   (noahsundberg@gmail.com is the PLATFORM_ADMIN on record), critical
   metrics to watch (`/healthz`, websocket count on `BINGO_BOARD` DO,
   Sentry error rate, D1 query time), and contact tree.

2. **`docs/dismissal-day-checklist.md`** — one-pager an admin prints and
   keeps at the front desk. Pre-dismissal (check board loads, have
   printed master list on hand). During dismissal (what "the app is down"
   looks like, the 30-second call-Noah-and-switch-to-paper drill).
   Post-incident (report the outage, replay the paper list into the
   board).

## Files expected to touch

- `docs/ops-runbook.md` (new)
- `docs/dismissal-day-checklist.md` (new)
- `docs/nightly-queue.md` (mark item 4 `[x]`, add follow-up for 0d harness)
- `docs/nightly/2026-04-24-build.md` (this file)

## Acceptance criteria

- Runbook reads like something an on-call human can follow at 3:05pm when
  the board isn't loading. Specific commands (`wrangler rollback`,
  `wrangler deployments list`, `/admin/print/master` URL) included.
- Dismissal-day checklist fits on one printable page and uses short,
  scannable steps — no 5-paragraph prose blocks.
- No code changes. `typecheck` and `test` are no-ops for this diff; the
  staging smoke gate should still run and pass because the build and
  deploy aren't affected.
- Summary updated at the end with MERGED commit range or BLOCKED reason.
