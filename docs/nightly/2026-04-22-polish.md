# Nightly polish — 2026-04-22

**Agent:** polish (2 AM nightly).
**Charter:** Part A — test coverage for tonight's build. Part B — research
spec for the next queued workstream. Part C — this summary.

## Outcome at a glance

- **Part A (test coverage):** SKIPPED. No `docs/nightly/2026-04-22-build.md`
  exists. The last build summary on disk is `2026-04-21-build.md`, which
  belongs to last night's run. Per the task rules ("If it doesn't exist,
  or it says the build was blocked/skipped, skip to Part B"), Part A was
  not executed. No new test files were added and no Playwright run was
  triggered.
- **Part B (research spec):** DONE. Drafted
  `docs/nightly-specs/mobile-smoke-sweep.md` for workstream `0b` on a new
  research branch `nightly-research/2026-04-22-mobile-smoke-sweep`. The
  branch is local-only — see "Push + PR" below.
- **Part C (this file):** DONE.

## Part A — why test coverage was skipped

`ls docs/nightly/` returned only `README.md` and `2026-04-21-build.md`.
There is no `2026-04-22-build.md`, which is the signal the task rules
use to decide whether a tonight build shipped. The branch
`nightly-build/2026-04-21-0a-smoke-test-sweep` still exists locally and
has four committed tests + fixmes from yesterday's run, but those
already have their tests (they ARE the tests), and they were already
summarized in `2026-04-21-build.md`. Writing more tests for them now
would be out of scope for this charter.

**Repo state note (non-blocking, flag for Noah):** the working tree on
`nightly-build/2026-04-21-0a-smoke-test-sweep` contains a large pile
of uncommitted work unrelated to the 0a test sweep — a whole "fire-drill
→ drills" rename across routes, domain, schema, migrations, a new
`drills.live.tsx`, a new admin `history.tsx`, plus docs
(`drill-templates-proposal.md`, `rfid-vehicle-tags.md`) and an untracked
`e2e/drills.spec.ts`. None of it is this polish agent's concern, and
it's been left untouched (this agent did its work in a separate
worktree off master). It looks like work-in-progress from a prior
session that didn't get committed — Noah, you may want to decide
whether to commit, stash, or discard it.

## Part B — research spec drafted

**Workstream picked:** `0b mobile-smoke-sweep` — first `[ ]` item in
`docs/nightly-queue.md` whose deps are met (depends on `0a`, which is
`[x]`).

**Spec file:** `docs/nightly-specs/mobile-smoke-sweep.md` (269 lines).

**Spec covers:**
- Exact Playwright config delta (add two projects —
  `mobile-iphone`/`mobile-android` — with `testMatch` scoped so CI cost
  stays flat).
- File to create: `e2e/smoke.mobile.spec.ts`. Routes to include,
  routes to skip, and why.
- Three mobile-only targeted checks on top of the route sweep:
  admin hamburger/drawer contract, marketing landing
  `scrollWidth <= clientWidth` guard, and `MobileCallerView` render
  path.
- Which tests are expected to `test.fixme` on day one (the same `/status`
  and `/api/healthz` middleware bugs from 0a, plus two new fixmes for
  tests that need a seeded admin session or tenant host — fixture gaps
  owned by `0d`).
- 12-behavior crib-sheet table for the build agent to follow.
- Four open questions flagged for Noah — most importantly, whether to
  add WebKit (Mobile Safari) coverage or stick to Chromium-simulated
  mobile.

**Branch + commit:**
- Base: `master` (`543fba5 update pricing`).
- Branch: `nightly-research/2026-04-22-mobile-smoke-sweep`.
- Commit: `804eaa9 docs(nightly): spec for mobile-smoke-sweep`
  (269 insertions, one new file).

### Push + PR

**NOT PUSHED, NO PR OPENED.** Two blockers, both environmental:

1. `git push origin HEAD` → `Host key verification failed. fatal: Could
   not read from remote repository.` Same SSH-known-hosts problem that
   blocked yesterday's build agent. Until Noah adds the GitHub host
   keys to the sandbox's `known_hosts` (or switches the remote to
   HTTPS with a PAT), nightly agents cannot push.
2. `gh` is not installed in this sandbox (`command not found`). Same
   gap yesterday flagged. Even after a successful push, a draft PR
   can't be opened without `gh` or a manual follow-up.

The branch is local-only. Noah can push it with a one-liner:

```bash
cd /path/to/school-organizer
git push -u origin nightly-research/2026-04-22-mobile-smoke-sweep
# Then open a draft PR via the GitHub web UI, or install gh:
# gh pr create --draft --title "docs(nightly): spec for mobile-smoke-sweep" \
#   --body "See docs/nightly-specs/mobile-smoke-sweep.md"
```

## Filesystem caveats hit during this run (Noah-awareness)

The sandbox repo's `.git/` sits on a fuse mount that rejects `unlink()`
on lock files. `git worktree add` and `git commit` produced a handful
of `warning: unable to unlink '.../HEAD.lock': Operation not permitted`
messages, and one commit attempt had to be retried after renaming the
stale locks (`HEAD.lock` → `HEAD.lock.stale2`, same for
`refs/heads/nightly-research/….lock` and `index.lock`). The commit
ultimately landed cleanly — `git log` shows `804eaa9` as the tip of
the research branch — but the noise is concerning. If it recurs, Noah
may want to remount the repo on a normal filesystem for the nightly
sandbox. Yesterday's build agent hit the same symptom
(`broken ref refs/stash.lock.stale`, `SQLITE_IOERR_DELETE`).

## Artifacts

- `docs/nightly-specs/mobile-smoke-sweep.md` — the spec.
- `docs/nightly/2026-04-22-polish.md` — this file.

## Next build agent's job

Pick `0b mobile-smoke-sweep` from `docs/nightly-queue.md`, read the spec
at `docs/nightly-specs/mobile-smoke-sweep.md`, and implement it on a
branch `nightly-build/2026-04-23-0b-mobile-smoke-sweep`. Open the four
"Open questions for Noah" early so they don't block the build — sane
defaults are already noted in the spec if Noah hasn't responded by
build time.
