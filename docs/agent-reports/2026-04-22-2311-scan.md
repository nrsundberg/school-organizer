# Staging scan — Wed Apr 22 23:11 EDT 2026

**Environment:** https://school-organizer-staging.sundbergne.workers.dev
**Master SHA:** 84ff6fe
**Summary:** 1 finding (1 P0, 0 P1, 0 P2) — scan was **blocked** at the environment layer, not a product defect.

## Findings

### [P0] Scanner cannot reach staging — proxy allowlist rejects the host
- **Where:** n/a (network layer, from the scanner sandbox)
- **What:** All outbound requests to `school-organizer-staging.sundbergne.workers.dev` are refused by the sandbox HTTP proxy before they reach Cloudflare, so no smoke sweep, no flow test, and no console/network capture could be performed this run.
- **Evidence:**
  - `curl -v https://school-organizer-staging.sundbergne.workers.dev/` →
    `HTTP/1.1 403 Forbidden` from the local proxy at `127.0.0.1:3128`, with header `X-Proxy-Error: blocked-by-allowlist`.
  - `nslookup school-organizer-staging.sundbergne.workers.dev` → `network unreachable` (DNS is also sandboxed; traffic must go through the proxy, which refuses this host).
  - `npx playwright test --config=playwright.staging.config.ts --project=chromium` produced a zero-byte `/tmp/scan-results.json` after hitting the 300s wrapper timeout — consistent with every request being blackholed by the proxy.
  - `git fetch origin` / `git pull` also failed with `Host key verification failed`, so the repo could not be synced to latest `origin/master`; this run scanned whatever master was already checked out locally (`84ff6fe`, which is already 1 commit ahead of `origin/master`).
- **Reproduction:** from the scanner sandbox, any of:
  ```
  curl -sv https://school-organizer-staging.sundbergne.workers.dev/
  PLAYWRIGHT_BASE_URL=https://school-organizer-staging.sundbergne.workers.dev \
    npx playwright test --config=playwright.staging.config.ts --project=chromium
  ```
- **Suspected cause:** The scheduled-task runner's egress allowlist does not include `*.sundbergne.workers.dev` (and the git remote's SSH host key is not trusted). Product code on staging may well be healthy — this report cannot say either way.
- **Status:** open — needs operator action, not a code fix. Add the staging hostname (and the git remote host) to the scanner's allowlist, or run the scanner from an environment with unrestricted egress. Re-running the scan after that unblocks every other check.

## What was checked
- **Attempted route sweep** (blocked): `e2e/` smoke suite against `PLAYWRIGHT_BASE_URL=https://school-organizer-staging.sundbergne.workers.dev` — every request 403'd at the proxy.
- **Attempted critical flow** (blocked): `/ → /pricing → click plan → /signup?plan=car-line → fill step 1 → Continue`. Not attempted after the initial connectivity check failed; there was no point writing `/tmp/flow.spec.ts` against an unreachable host.
- **Git sync** (blocked): `git fetch origin --prune` and `git pull --ff-only origin master` both failed with `Host key verification failed`. Local `master` is `84ff6fe` and is already 1 commit ahead of `origin/master`; this report is written against that local HEAD.
- **Chromium install** (succeeded): `npx playwright install chromium` completed — environment is otherwise ready to run as soon as egress is opened.

## Notes for the fixer
Please do not treat this as a product bug and do not attempt a source-code fix. Re-run the scanner after the allowlist is updated; if that run turns up real findings, they'll appear in a subsequent `docs/agent-reports/…-scan.md`.
