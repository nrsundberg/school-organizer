# Nightly Workstream Specs

Detailed specs for workstreams in `docs/nightly-queue.md` that need more than the queue's inline scope. The polish agent writes these during the research portion of its 2 AM run, so the next build agent has a clearer brief.

Naming: `{slug}.md` matching the queue slug.
