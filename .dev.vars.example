# Copy this to .dev.vars and fill in the values
# .dev.vars is used by wrangler for local secrets (gitignored)
BETTER_AUTH_SECRET=generate-a-random-secret-here
SENTRY_AUTH_TOKEN=your-sentry-auth-token
